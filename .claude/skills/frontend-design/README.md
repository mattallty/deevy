# frontend-design skill (vendored)

Anthropic's `frontend-design` skill, copied on 2026-09-05 from the `claude-plugins-official` marketplace
plugin of the same name (`SKILL.md` and its `LICENSE.txt`, unchanged). It is the design-direction pass —
typography, palette, anti-template rules, plan-then-critique — that the deevy UI redesign follows; the
decisions it produced for deevy live in `../deevy-ui/SKILL.md`, which is the one to read first.
