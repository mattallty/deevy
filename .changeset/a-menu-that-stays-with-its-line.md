---
"@deevy/web": patch
---

The `/` block menu and the `@` mention list stay with the line they were opened on. Scrolling used to leave
them where they were on screen, so the menu drifted away from the words it was about.
