---
---

Internal: the list and the board build their groups from one `Grouping` object rather than from two
State-shaped loops, which is what lets a later slice group by anything else. Nothing on screen changes.
