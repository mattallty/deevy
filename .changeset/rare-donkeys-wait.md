---
"@deevy/release": patch
---

Releasing no longer tags the version pull request's own commit, the version pull request can pass its own
checks, and a prerelease tag no longer moves the `latest` image tag.
