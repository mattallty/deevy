A Document, a description or a comment, rendered from markdown: the typography of everything Humans and Agents write (14px/24px, headings 20/16/14, code in mono on `bg-muted`, task lists, tables, blockquotes). Read mode and the editor share it so switching does not reflow. Use it for any body text a Member wrote; never re-style prose by hand.

```tsx
<Markdown>{"## Problem\n\nThe Gate has no ruling authority.\n\n- [ ] decide who rules"}</Markdown>
```
